apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argo-setup
spec:
  destination:
    name: in-cluster
    namespace: argocd
  source:
    path: argo-setup
    repoURL: 'https://github.com/alejo-maya/app-of-apps-2'
    targetRevision: HEAD
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
