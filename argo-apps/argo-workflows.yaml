apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argo-workflows-setup
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/alejo-maya/argo-apps.git
    targetRevision: HEAD
    path: argo-apps/argo-workflows
  destination: 
    server: https://kubernetes.default.svc
    namespace: argo
  syncPolicy:
    syncOptions:
    - CreateNamespace=false
    automated:
      selfHeal: true
      prune: true
